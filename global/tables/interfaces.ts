export interface limitsData {
  dev?: boolean,
  persistence?: boolean,
  setLimitItems?: boolean,
  setLimitPages?: boolean,
  limitItems?: number,
  limitPages?: number,
  setLang?: boolean,
  chooseLang?: string,
}

export interface customQuery {
  setLanguage?: string,
}